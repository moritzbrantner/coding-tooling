name: Temporary tauri-template fast reusable probe

on:
  push:
    branches:
      - tmp/tauri-template-pr12-lock

permissions:
  contents: read
  packages: read

jobs:
  probe:
    uses: moritzbrantner/reusable-workflows/.github/workflows/coding-tooling-validation.yml@8da6ad737d4746159414bde686d5c7422e0a9232
    with:
      operation: run
      tier: fast
      component: coding-tooling
      strict: true
      install_mode: auto
