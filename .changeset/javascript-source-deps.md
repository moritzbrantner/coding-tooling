# JavaScript source dependencies

`coding-tooling source-deps` now has a schema-v4 JavaScript/Bun adapter for exact local package sources. It keeps ordinary dependency declarations intact while sharing prepare, status, smoke, restore, and exact source-graph behavior across repositories.
